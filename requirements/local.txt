# Local edition optional/runtime dependencies live here.
# Install these in the shared builder venv only when building/testing Local.
# Lite build excludes these heavy Local modules.

# comic_text_detector runtime
torch>=2.1.0
torchvision>=0.16.0
tqdm
pyclipper
shapely
PyYAML
networkx

# PaddleOCR runtime
# PaddleOCR package does not always install the Paddle runtime by itself.
# Keep PaddlePaddle pinned here so Local build venv can be created from scratch.
# 3.3.x CPU는 oneDNN/PIR OCR 오류가 날 수 있으므로 3.2.2로 고정합니다.
paddlepaddle-gpu>=2.5.0
paddleocr[doc-parser]>=2.9.0
# PaddleX/PaddleOCR imports pandas dynamically at runtime. Keep it explicit so
# the Local frozen package does not lose OCR text recognition.
pandas

# Local LaMa inpainting runtime
# simple-lama-inpainting requires numpy < 2.0; this version also satisfies PaddleOCR/PaddleX.
numpy==1.26.4
setuptools==81.0.0
simple-lama-inpainting

# SDXL Lightning & 디퓨전 생태계
diffusers>=0.26.0
accelerate>=0.27.0
peft>=0.8.0
safetensors>=0.4.0

# Local Manga OCR runtime (dev/source mode)
manga-ocr==0.1.14
transformers>=4.40.0,<5.0.0
sentencepiece>=0.2.0
protobuf>=4.25.0
